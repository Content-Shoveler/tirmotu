import * as HeroUI from '@heroui/react';

// Log all exported components to console
console.log('Available HeroUI exports:', Object.keys(HeroUI));

// Export for reference
export const HeroUIExports = Object.keys(HeroUI);
